import React from 'react';

let SignIn = () => {
	return <React.Fragment>
		SignIn
	</React.Fragment>
};

SignIn = React.memo(SignIn);
SignIn.defaultProps = {
};
SignIn.propTypes = {
};

export default SignIn;
